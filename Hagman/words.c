#include "words.h" //Include the custom header containing the function definitions
#include <stdlib.h>
#include <time.h> //For rand()

const char *getWord()
{
    char wordList[500][5] = {{}};
}
// int num = (rand() %
//            (upper - lower + 1)) + lower;
